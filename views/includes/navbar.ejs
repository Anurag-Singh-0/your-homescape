<nav class="navbar navbar-expand-md bg-body-light border-bottom">
  <div class="container-fluid">
    <a href="/listings"><img src="/images/HomeScape.png" alt="Logo" /></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a href="/listings" class="nav-link">Home</a>
        <a href="/listings/new" class="nav-link">Add listing</a>
      </div>

      <div class="navbar-nav ms-auto">      
        <% if (!currentUser) { %>
        <a href="/signup" class="nav-link">Sign up</a>
        <a href="/login" class="nav-link">Log in</a>
        <% } %>

        <% if (currentUser) { %>
          <div id="navUserLogo"></div><span class="nav-link text-dark navUserText">Hello, <%= currentUser.username %></span>
          <a href="/logout" class="nav-link">Log out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
